global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "fastapi-app"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["api:8080"] # 'api' matches the container name in docker-compose.yml

  - job_name: "cadvisor"
    static_configs:
      - targets: ["cadvisor:8080"]
